# Copy this file to `.env` and set REPO_ROOT to the absolute path of your
# local clone of this repository (the git folder that contains the `tools` package).
# The tools load this with python-dotenv so they can be run from any working
# directory and still import the `tools` package.
REPO_ROOT=/absolute/path/to/parag-climate-information
